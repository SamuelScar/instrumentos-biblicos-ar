name: instrumentos-biblicos-ar

services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: development
    init: true
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
